# Optional: separately hosted runtime character directory, no trailing file name.
# Applied to both development and production builds; leave unset for local dev defaults.
# Do not place secrets here: every VITE_* setting is visible to the browser.
# For an all-in-one deployment artifact, use npm run build:characters instead.
# VITE_CHARACTER_ASSET_BASE=https://your-asset-host.example/characters
